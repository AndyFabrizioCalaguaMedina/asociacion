<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cursos | Asociación Privada de Patronato</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Iconos -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <!-- CSS local -->
  <link rel="stylesheet" href="/styles/stylesservicio.css">
</head>

<body>

  <!-- 🔹 NAVBAR -->
  <header id="mainHeader">
    <nav class="navbar">
      <div class="logo" id="logoToggle">
        <i class="fa-solid fa-scissors"></i>
        <a href="/" class="logo-text">PATRONATO</a>
      </div>

      <div class="nav-links" id="navMenu">
        <li class="nav-item"><a class="nav-link" href="nosotros">Nosotros</a></li>
        <li class="nav-item"><a class="nav-link active" href="servicios">Cursos</a></li>
        <li class="nav-item"><a class="nav-link" href="matricula">Matrícula</a></li>
        <li class="nav-item"><a class="nav-link" href="contacto">Contacto</a></li>
      </div>
    </nav>
  </header>

  <!-- HERO CURSOS -->
  <section class="hero-cursos">
    <div class="container">
      <div class="row justify-content-center text-center">
        <div class="col-12 col-lg-8">
          <h1 class="fw-bold mb-4">NUESTROS CURSOS</h1>
          <p class="hero-subtitle lead">
            Descubre nuestra oferta educativa especializada en belleza y estética. 
            Formación profesional con los mejores instructores y equipos de última generación.
          </p>
        </div>
      </div>
    </div>
  </section>

<!-- SECCIÓN DE CURSOS - GRID CARDS -->
<section id="cursos" class="cursos-grid-section py-5 py-lg-6 bg-light">
  <div class="container">
    <div class="row justify-content-center text-center mb-5">
      <div class="col-12 col-lg-8">
        <h2 class="fw-bold display-5 mb-4">NUESTROS PROGRAMAS</h2>
        <p class="lead">
          Elige el curso que mejor se adapte a tus intereses y comienza tu camino en el mundo de la belleza
        </p>
      </div>
    </div>

    <div class="row g-4">
      <!-- Cambia de col-md-6 col-lg-3 a col-md-6 para 2x2 -->
      
      <!-- Barbería Avanzada -->
      <div class="col-md-6"> <!-- Cambiado aquí -->
        <div class="curso-card">
          <div class="curso-video-card">
            <video autoplay muted loop playsinline>
              <source src="videos/Barberia.mp4" type="video/mp4">
              Tu navegador no soporta el elemento video.
            </video>
          </div>
          <div class="curso-content">
            <h3 class="curso-titulo-card">Barbería Avanzada</h3>
          </div>
        </div>
      </div>

      <!-- Maquillaje Profesional -->
      <div class="col-md-6"> <!-- Cambiado aquí -->
        <div class="curso-card">
          <div class="curso-video-card">
            <video autoplay muted loop playsinline>
              <source src="videos/Maquillaje.mp4" type="video/mp4">
              Tu navegador no soporta el elemento video.
            </video>
          </div>
          <div class="curso-content">
            <h3 class="curso-titulo-card">Maquillaje Profesional</h3>
          </div>
        </div>
      </div>

      <!-- Diseño de Cejas -->
      <div class="col-md-6"> <!-- Cambiado aquí -->
        <div class="curso-card">
          <div class="curso-video-card">
            <video autoplay muted loop playsinline>
              <source src="videos/Diseño de cejas y pestañas.mp4" type="video/mp4">
              Tu navegador no soporta el elemento video.
            </video>
          </div>
          <div class="curso-content">
            <h3 class="curso-titulo-card">Diseño de Cejas</h3>
          </div>
        </div>
      </div>

      <!-- Diseño de Uñas -->
      <div class="col-md-6"> <!-- Cambiado aquí -->
        <div class="curso-card">
          <div class="curso-video-card">
            <video autoplay muted loop playsinline>
              <source src="videos/Diseño de uñas.mp4" type="video/mp4">
              Tu navegador no soporta el elemento video.
            </video>
          </div>
          <div class="curso-content">
            <h3 class="curso-titulo-card">Diseño de Uñas</h3>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

  <!-- CARRUSEL GALERÍA DE TRABAJOS -->
  <section class="galeria-trabajos py-5 py-lg-6">
    <div class="container">
      <div class="row justify-content-center text-center mb-5">
        <div class="col-12 col-lg-8">
          <h2 class="fw-bold display-5 mb-4">GALERÍA DE TRABAJOS</h2>
          <p class="lead">
            Mira algunos de los increíbles trabajos realizados por nuestros estudiantes y egresados
          </p>
        </div>
      </div>

      <!-- Carrusel -->
      <div class="position-relative">
        <div id="galeriaContainer" class="overflow-hidden">
          <div class="d-flex galeria-track" style="transition: transform 0.5s ease;">
            <!-- Imágenes del carrusel -->
            <div class="galeria-item flex-shrink-0 me-3">
              <img src="images/Trabajo 1.webp" class="d-block w-100 rounded-3" alt="Trabajo 1" loading="lazy">
            </div>
            <!--<div class="galeria-item flex-shrink-0 me-3">
              <img src="images/Trabajo 2.webp" class="d-block w-100 rounded-3" alt="Trabajo 2" loading="lazy">
            </div>-->
            <div class="galeria-item flex-shrink-0 me-3">
              <img src="images/Trabajo 3.webp" class="d-block w-100 rounded-3" alt="Trabajo 3" loading="lazy">
            </div>
            <div class="galeria-item flex-shrink-0 me-3">
              <img src="images/Trabajo 4.webp" class="d-block w-100 rounded-3" alt="Trabajo 4" loading="lazy">
            </div>
            <div class="galeria-item flex-shrink-0 me-3">
              <img src="images/Trabajo 5.webp" class="d-block w-100 rounded-3" alt="Trabajo 5" loading="lazy">
            </div>
            <div class="galeria-item flex-shrink-0 me-3">
              <img src="images/Trabajo 6.webp" class="d-block w-100 rounded-3" alt="Trabajo 6" loading="lazy">
            </div>
            <div class="galeria-item flex-shrink-0 me-3">
              <img src="images/Trabajo 7.webp" class="d-block w-100 rounded-3" alt="Trabajo 6" loading="lazy">
            </div>
            <div class="galeria-item flex-shrink-0 me-3">
              <img src="images/Trabajo 8.webp" class="d-block w-100 rounded-3" alt="Trabajo 6" loading="lazy">
            </div>
            <div class="galeria-item flex-shrink-0 me-3">
              <img src="images/Trabajo 9.webp" class="d-block w-100 rounded-3" alt="Trabajo 6" loading="lazy">
            </div>
            <div class="galeria-item flex-shrink-0 me-3">
              <img src="images/Trabajo 10.webp" class="d-block w-100 rounded-3" alt="Trabajo 6" loading="lazy">
            </div>
          </div>
        </div>

        <!-- Controles del carrusel -->
        <button class="carousel-control-prev position-absolute top-50 start-0 translate-middle-y" type="button" id="prevBtn">
          <span class="carousel-control-prev-icon bg-dark rounded-circle p-2"></span>
          <span class="visually-hidden">Anterior</span>
        </button>
        <button class="carousel-control-next position-absolute top-50 end-0 translate-middle-y" type="button" id="nextBtn">
          <span class="carousel-control-next-icon bg-dark rounded-circle p-2"></span>
          <span class="visually-hidden">Siguiente</span>
        </button>
      </div>
    </div>
  </section>

  <!-- SECCIÓN INFORMACIÓN ADICIONAL -->
  <section class="info-cursos py-5 py-lg-6">
    <div class="container">
      <div class="row justify-content-center text-center mb-5">
        <div class="col-12 col-lg-8">
          <h2 class="fw-bold display-5 mb-4">¿POR QUÉ ELEGIRNOS?</h2>
          <p class="lead">
            Ofrecemos la mejor formación en belleza y estética con beneficios exclusivos para nuestros estudiantes
          </p>
        </div>
      </div>

      <div class="row g-4">
        <!-- Instructores -->
        <div class="col-md-4">
          <div class="beneficio-curso bg-white text-center p-4 rounded">
            <i class="bi bi-people-fill text-celeste fs-1 mb-3"></i>
            <h4 class="fw-bold mb-3">Instructores Expertos</h4>
            <p class="text-muted">Aprende con profesionales activos en la industria con años de experiencia.</p>
          </div>
        </div>

        <!-- Equipos -->
        <div class="col-md-4">
          <div class="beneficio-curso bg-white text-center p-4 rounded">
            <i class="bi bi-tools text-celeste fs-1 mb-3"></i>
            <h4 class="fw-bold mb-3">Equipos Modernos</h4>
            <p class="text-muted">Trabaja con equipos de última generación y productos de alta calidad.</p>
          </div>
        </div>

        <!-- Certificación -->
        <div class="col-md-4">
          <div class="beneficio-curso bg-white text-center p-4 rounded">
            <i class="bi bi-award-fill text-celeste fs-1 mb-3"></i>
            <h4 class="fw-bold mb-3">Certificación Oficial</h4>
            <p class="text-muted">Obtén un diploma reconocido que avala tus conocimientos y habilidades.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- SECCIÓN MATRÍCULA -->
  <section class="matricula-cursos py-5 py-lg-6 bg-dark text-white">
    <div class="container">
      <div class="row justify-content-center text-center">
        <div class="col-12 col-lg-8">
          <div class="matricula-cursos-content">
            <h2 class="fw-bold display-5 mb-4">¿LISTO PARA COMENZAR?</h2>
            <p class="lead mb-5">
              No esperes más para transformar tu pasión en profesión. 
              Contáctanos para recibir información detallada sobre horarios, costos y proceso de matrícula.
            </p>
            
            <div class="matricula-acciones">
              <a href="matricula" class="btn btn-primary btn-lg px-5 py-3 fw-bold me-3 mb-3">
                <i class="bi bi-pencil-fill me-2"></i>Ver Matrícula
              </a>
              <a href="contacto" class="btn btn-outline-light btn-lg px-5 py-3 fw-bold mb-3">
                <i class="bi bi-chat-left-text me-2"></i>Contactar
              </a>
            </div>
            
            <div class="matricula-info mt-4">
              <p class="small">
                <i class="bi bi-info-circle me-2"></i>
                Todos nuestros cursos incluyen materiales de trabajo y certificación al finalizar
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

<br>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="footer-container">
      <div class="footer-grid">
        <!-- Contacto -->
        <div class="footer-section">
          <h3>Contacto</h3>
          <ul class="contact-info">
            <li>
              <i class="fas fa-map-marker-alt"></i>
              <span>Av. Alejandro Bertello, Callao</span>
            </li>
            <li>
              <i class="fas fa-phone"></i>
              <span>+51 987 654 321</span>
            </li>
            <li>
              <i class="fas fa-envelope"></i>
              <span>info@patronato.com</span>
            </li>
          </ul>
        </div>

        <!-- Horarios -->
        <div class="footer-section">
          <h3>Horarios</h3>
          <ul class="hours-list">
            <li>Lunes a Viernes</li>
            <li>8:00 - 16:00</li>
            <li style="margin-top: 12px;">Sábados</li>
            <li>8:00 - 14:00</li>
          </ul>
        </div>

        <!-- Cursos -->
        <div class="footer-section">
          <h3>Cursos</h3>
          <ul>
            <li><a href="servicios">Barbería Avanzada</a></li>
            <li><a href="servicios">Maquillaje Profesional</a></li>
            <li><a href="servicios">Diseño de Cejas</a></li>
            <li><a href="servicios">Diseño de Uñas</a></li>
          </ul>
        </div>

        <!-- Promoción -->
        <div class="footer-section">
          <h3>Promoción Especial</h3>
          <div class="promo-box">
            <p>Inscríbete ahora y obtén un descuento exclusivo en todos nuestros cursos.</p>
          </div>
          <p style="font-size: 0.9rem; color: #a0a0a0;">Válido por tiempo limitado</p>

          <div class="social-links">
            <a href="#"><i class="fab fa-instagram"></i></a>
            <a href="#"><i class="fab fa-facebook-f"></i></a>
            <a href="#"><i class="fab fa-tiktok"></i></a>
            <a href="#"><i class="fab fa-whatsapp"></i></a>
          </div>
        </div>
      </div>

      <div class="footer-bottom">
        <p>&copy; 2025 Asociación Privada de Patronato. Todos los derechos reservados.</p>
      </div>
    </div>
  </footer>

  <!-- Scripts -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // JavaScript para el funcionamiento del menú móvil
    document.addEventListener('DOMContentLoaded', function () {
      const logoIcon = document.querySelector('.logo i');
      const logoText = document.querySelector('.logo');
      const navMenu = document.getElementById('navMenu');
      const menuOverlay = document.createElement('div');
      menuOverlay.className = 'menu-overlay';
      document.body.appendChild(menuOverlay);

      let isMenuOpen = false;

      // Función para abrir/cerrar el menú
      function toggleMenu() {
        isMenuOpen = !isMenuOpen;
        navMenu.classList.toggle('active');
        menuOverlay.classList.toggle('active');
        document.body.classList.toggle('menu-open');

        // Animación del icono
        if (isMenuOpen) {
          logoIcon.style.transform = 'rotate(90deg)';
          logoIcon.style.color = 'var(--celeste-suave)';
        } else {
          logoIcon.style.transform = 'rotate(0deg)';
          logoIcon.style.color = 'var(--celeste)';
        }
      }

      // Evento para el icono de tijeras (hamburguesa) - SOLO en móvil
      function handleIconClick(e) {
        if (window.innerWidth <= 992) {
          e.preventDefault();
          e.stopPropagation();
          toggleMenu();
        }
      }

      // Evento para el texto del logo - SIEMPRE redirige al index
      function handleLogoClick(e) {
        if (window.innerWidth <= 992 && e.target.tagName === 'I') {
          return;
        }
        window.location.href = '/';
      }

      // Agregar event listeners
      logoIcon.addEventListener('click', handleIconClick);
      logoText.addEventListener('click', handleLogoClick);

      // Cerrar menú al hacer clic en el overlay
      menuOverlay.addEventListener('click', function () {
        if (window.innerWidth <= 992 && isMenuOpen) {
          toggleMenu();
        }
      });

      // Cerrar menú al hacer clic en un enlace (solo en móvil)
      const navLinks = document.querySelectorAll('.nav-link');
      navLinks.forEach(link => {
        link.addEventListener('click', function () {
          if (window.innerWidth <= 992 && isMenuOpen) {
            toggleMenu();
          }
        });
      });

      // Cerrar menú con la tecla ESC (solo en móvil)
      document.addEventListener('keydown', function (e) {
        if (e.key === 'Escape' && window.innerWidth <= 992 && isMenuOpen) {
          toggleMenu();
        }
      });

      // Cerrar menú al redimensionar la ventana a desktop
      window.addEventListener('resize', function () {
        if (window.innerWidth > 992 && isMenuOpen) {
          toggleMenu();
        }
      });

      // Restaurar estado del icono al cargar la página
      logoIcon.style.transform = 'rotate(0deg)';
      logoIcon.style.color = 'var(--celeste)';

      // Scroll header effect
      window.addEventListener('scroll', () => {
        const header = document.getElementById('mainHeader');
        header.classList.toggle('scrolled', window.scrollY > 50);
      });

      // JavaScript para el carrusel de galería
      const track = document.querySelector('.galeria-track');
      const prevBtn = document.getElementById('prevBtn');
      const nextBtn = document.getElementById('nextBtn');
      const items = document.querySelectorAll('.galeria-item');
      const itemWidth = items[0].offsetWidth + 12; // width + margin
      let currentPosition = 0;

      function updateCarousel() {
        track.style.transform = `translateX(${currentPosition}px)`;
      }

      nextBtn.addEventListener('click', () => {
        const maxPosition = -((items.length - 4) * itemWidth);
        currentPosition = Math.max(currentPosition - itemWidth, maxPosition);
        updateCarousel();
      });

      prevBtn.addEventListener('click', () => {
        currentPosition = Math.min(currentPosition + itemWidth, 0);
        updateCarousel();
      });

      // Ajustar en resize
      window.addEventListener('resize', () => {
        const newItemWidth = items[0].offsetWidth + 12;
        currentPosition = 0;
        updateCarousel();
      });
    });
  </script>
</body>
</html>